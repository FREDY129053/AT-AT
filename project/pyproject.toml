[project]
name = "project"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
  "apps/backend",
  "apps/ab_agent",
  "apps/api_agent",
  "packages/*",
]
exclude = [
  "apps/frontend",
]